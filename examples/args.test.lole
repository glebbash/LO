(mod lib/cli)
(mod lib/args)

(fn main [] [] (
    {args : Args = (Args/load)}

    {i : u32 = 0}
    (loop (
        (if {i == {args . argc}} (break))

        {arg : StringSlice = (Args/get args i)}

        (print_str_slice arg)
        (println)

        {i = {i + 1}}
    ))
))
