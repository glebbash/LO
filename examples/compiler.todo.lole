(:: compile (i32 i32) (i32))
(fn compile (script_ptr script_len) (
    ; TODO: implement
    (i32.const 42)
))

(:: free_binary (i32) ())
(fn free_binary (wasm_binary) (
    ; TODO: implement
))

(export compile :as compile)
(export free_binary :as free_binary)
