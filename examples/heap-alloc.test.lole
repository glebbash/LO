(mod lib/cli)

(fn main [] [] (
    (print_str_slice "Heap/TOTAL_ALLOCATED = ")
    (print_u32 Heap/TOTAL_ALLOCATED) (println)

    {&p : ptr = (Heap/alloc 1)}
    (print_str_slice "&p = (Heap/alloc 1) // ")
    (print_u32 &p) (println)

    (print_str_slice "(Heap/free &p)\n")
    (Heap/free &p)

    {&p = (Heap/alloc 1)}
    (print_str_slice "&p = (Heap/alloc 1) // ")
    (print_u32 &p) (println)

    {&p = (Heap/alloc 1)}
    (print_str_slice "&p = (Heap/alloc 1) // ")
    (print_u32 &p) (println)
))
