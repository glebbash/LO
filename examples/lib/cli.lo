@include("./std.lo", with_extern)
@include("./wasi.lo", with_extern)
@include("./print.lo", with_extern)

// 1MB stack + 1MB heap
@use_memory(min_pages = 40, exported)

@export_existing(main, from_root, out = "_start")
