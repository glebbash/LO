include "./std.lo" with extern
include "./wasi.lo" with extern
include "./print.lo" with extern

// 1MB stack + 1MB heap
@use_memory(min_pages = 40, exported)

@export_existing(main, from_root, out = "_start")
