include "./std.lo" with extern
include "./wasi.lo" with extern
include "./print.lo" with extern

// 1MB stack + 1MB heap
export memory { min_pages: 40 }

@export_existing(
    in = "main",
    out = "_start",
    from_root = true,
)
