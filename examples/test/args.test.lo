@include("../lib/cli.lo")
@include("../lib/args.lo")

fn main() {
    let args = Args::load()
    // TODO: use for each
    for i in 0..args.argc {
        let arg = args.get(i)
        println(arg)
    }
}
