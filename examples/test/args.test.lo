include "lib/cli";
include "lib/args";

fn main() {
    let args = Args::load();

    let i = 0;
    loop {
        if i == args.argc {
            break;
        };

        let arg = args.get(i);
        puts(arg);
        puts("\n");

        i += 1;
    };
};
