(mod lib/cli)
(mod lib/args)

(fn main [] void (
    {args := (Args/load)}

    {i := 0}
    (loop (
        (if {i == {args . argc}} (break))

        {arg := (Args/get args i)}

        (puts arg)
        (puts "\n")

        {i += 1}
    ))
))
