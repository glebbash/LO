(mod lib/cli)
(mod lib/args)

(fn main [] [] (
    {args : Args = (Args/load)}

    {i := 0}
    (loop (
        (if {i == {args . argc}} (break))

        {arg : StringSlice = (Args/get args i)}

        (puts arg)
        (puts "\n")

        {i += 1}
    ))
))
