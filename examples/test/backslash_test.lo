fn backslash_test() {
    let x = 1

    // TODO(fmt): remove extra space at line end
    let x1 = x + 
        \ x + 
        \ x

    let x2 = (
        \ x
    ) + x

    let x3 = 2 + f(
        \ x
    ) + f(
        \ x
    )

    let x4 = x + 
        \ f(
            \ x
        ) + f(
            \ x
        )

    let x5 = x + f(x + f(x + f(x + f(x + f(x + f(x + f(x + f(x + f(
                                        \ x
                                    )
                                )
                            )
                        )
                    )
                )
            )
        )
    )

    if false {
        let x6 = x + 
            \ .s {
                x: 0,
            }.x + x
    }
    let x7 = x + 
        \ x + 
        \ (x + 
            \ x + .s {
                x: 0,
            }.x
        ) + x

    let x8 = x + 
        \ f(x + 
            \ x + 
            \ x
        )

    let x9 = f2(
        1 + 
            \ 1,
        1,
    )
}

struct s {
    x: u32,
}

fn f(x: u32): u32 {
    return .s { x: x }.x
}

fn f2(x: u32, y: u32): u32 {
    return x + y
}
