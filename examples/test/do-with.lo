include "../lib/cli.lo"

fn main() {
    do print(it.to_tmp_str!()) with (
        1, \\ 2, \\ 3,
        4, \\ 5, \\ 6,
        7, \\ 8, \\ 9,
    )

    print("\n-------------\n")

    do print(it) with (
        "{",
        " \"type\": \"file\",",
        " \"index\": ", \\ 1.to_tmp_str!(), \\ ",",
        " \"path\": \"", \\ "some_path.lo", \\ "\" ",
        "},",
    )

    print("\n")
}
