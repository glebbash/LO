include "lib/cli";

fn main() {
    puts("heap::TOTAL_ALLOCATED; // ");
    print_u32(heap::TOTAL_ALLOCATED); puts("\n");

    let p = heap::alloc(1);
    puts("let p = heap::alloc(1); // ");
    print_u32(p as u32); puts("\n");

    puts("heap::free(p);\n");
    heap::free(p);

    p = heap::alloc(1);
    puts("p = heap::alloc(1); // ");
    print_u32(p as u32); puts("\n");

    p = heap::alloc(1);
    puts("p = heap::alloc(1); // ");
    print_u32(p as u32); puts("\n");
};
