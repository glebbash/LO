@include("../lib/cli.lo")

fn main() {
    let p1 = heap::alloc(1)
    print("p1 = ")
    print((p1 as u32).to_tmp_str!())
    print("\n")

    heap::free(p1)

    let p2 = heap::alloc(1)
    print("p2 = ")
    print((p2 as u32).to_tmp_str!())
    print("\n")

    let p3 = heap::alloc(1)
    print("p3 = ")
    print((p3 as u32).to_tmp_str!())
    print("\n")
}
