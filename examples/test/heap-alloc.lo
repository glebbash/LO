include "lib/cli";

fn main() {
    puts("Heap_TOTAL_ALLOCATED; // ");
    print_u32(Heap_TOTAL_ALLOCATED); puts("\n");

    let p = Heap_alloc(1);
    puts("let p = Heap_alloc(1); // ");
    print_u32(p); puts("\n");

    puts("Heap_free(p);\n");
    Heap_free(p);

    p = Heap_alloc(1);
    puts("p = Heap_alloc(1); // ");
    print_u32(p); puts("\n");

    p = Heap_alloc(1);
    puts("p = Heap_alloc(1); // ");
    print_u32(p); puts("\n");
};
