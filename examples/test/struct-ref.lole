(mod lib/std)

(mem m :min 100)

(struct Point2D
    (x u32)
    (y u32)
)

(export main :as main)
(fn main [] u32 (
    {&p : (& Point2D) = (Stack/alloc (sizeof Point2D))}
    (defer (Stack/free (sizeof Point2D)))

    {(* &p) = (Point2D :x 2 :y 3)}
    {y := {&p -> y}}
    (return y)
))
