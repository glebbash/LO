include "../lib/cli.lo"

fn main() {
    with x in (1, 2, 3, 4, 5, 6, 7, 8, 9) do {
        puts(x.to_tmp_str!())
    }

    puts("\n-------------\n")

    // TODO: format like in `docs/exploration/backslashes2.lo`
    with x in (
        "{",
        " \"type\": \"file\",",
        " \"index\": ",
        1.to_tmp_str!(),
        ",",
        " \"path\": \"",
        "some_path.lo",
        "\" ",
        "},",
    ) do {
        puts(x)
    }

    puts("\n")
}
