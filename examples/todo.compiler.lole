(export mem_alloc :as mem_alloc)
(export mem_free :as mem_free)
(export compile :as compile)

; TODO: implement
(fn mem_alloc [(i32 size)] [i32] (
    0
))

; TODO: implement
(fn mem_free [(ptr i32) (size i32)] [] (
))

; TODO: implement
(fn compile [(script_ptr i32) (script_len i32)] [i32 i32 i32] (
    0
))
