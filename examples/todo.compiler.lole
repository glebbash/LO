(export mem_alloc :as mem_alloc)
(export mem_free :as mem_free)
(export compile :as compile)

; TODO: implement
(fn mem_alloc [(size u32)] [u32] (
    0
))

; TODO: implement
(fn mem_free [(ptr ptr) (size u32)] [] (
))

; TODO: implement
(fn compile [(script_ptr ptr) (script_len u32)] [u32 u32 u32] (
    0
))
