(:: mem_alloc (i32) (i32))
(:: mem_free (i32 i32) ())
(:: compile (i32 i32) (i32))

(export mem_alloc :as mem_alloc)
(export mem_free :as mem_free)
(export compile :as compile)

; TODO: implement
(fn mem_alloc (size) (
    (i32 0)
))

; TODO: implement
(fn mem_free (ptr size) (
    (return)
))

; TODO: implement
(fn compile (script_ptr script_len) (
    (i32 0)
))
