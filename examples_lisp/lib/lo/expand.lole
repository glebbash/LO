(struct Lo/Expand/ExpandResult
    (ok bool)
    (data (& void)) ; (& Vec<Lo/Ast/SExpr>) | (& Lo/Ast/CompileError)
)

; TODO: implement
(fn Lo/Expand/expand [
    (exprs (& Vec<Lo/Ast/SExpr>))
] Lo/Expand/ExpandResult (
    (return (Lo/Expand/ExpandResult
        :ok true
        :data {exprs as (& void)}
    ))
))
